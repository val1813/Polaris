# 贡献指南 — 什么能合，怎么审

> 不是什么都合。每类贡献有明确的合入标准。

---

## 贡献类型 → 合入标准

### 1. 知识图谱条目 → `knowledge_graph/`

**谁贡献：** 跑完一个课题的用户

**提交什么：** 一个 JSON 文件，按 `knowledge_graph/README.md` 格式

**你审什么（3 分钟）：**
```
[ ] GATE 7 格式完整？（project/version/date/status/nodes）
[ ] nodes 里有没有 ≥1 个 confidence ≥ 0.7 的条目？
[ ] 引用的 DOI/arXiv 是否真实？（抽查 1-2 个）
[ ] 不是纯 AI 幻觉？（Phase清单.md 存在 → 证明是 SOP 跑出来的）
```
三项全过 → 合。有疑问 → 标 `⚠️ unverified` 仍合，社区可以后续核实。

### 2. 领域 Agent 角色 → `contrib/`

**谁贡献：** 有领域专长的用户

**提交什么：** 一个 `.md` 文件，按 `contrib/README.md` 模板

**你审什么（5 分钟）：**
```
[ ] 角色定位清晰？（不是"通用改进"，是"凝聚态专项"）
[ ] 附了使用案例？（必须有一个真实课题的对比：有角色 vs 无角色）
[ ] 和现有角色不重复？（搜一下 contrib/ 有没有类似的）
[ ] 没有破坏性指令？（不要求改硬规则、不要求改 SOP）
```
过 → 合。这是 opt-in 的——不合入核心流程，只放在 contrib/ 供人选用。

### 3. 基准条目 → `benchmarks/`

**谁贡献：** 熟悉某个物理学领域的用户

**提交什么：** 一个 JSON 文件，按 `benchmarks/B01_*.json` 格式

**你审什么（5 分钟）：**
```
[ ] 矛盾是真实的吗？（proposition_A 和 B 确实不能同时为真）
[ ] 答案确实是已知的？（ground_truth 里的论文真实存在）
[ ] 难度标注合理？（easy/medium/hard）
[ ] 不是当前仍未解决的开放问题？
```
过 → 合。开放问题（还没答案的）不能当基准。

### 4. SOP 核心改动 → `research-group/` 或 `topic-selector/`

**谁贡献：** 深度使用过的用户

**提交什么：** PR 到核心文件

**你审什么（需要跑一遍）：**
```
[ ] PR 描述里附了 ≥2 个课题的对比结果？（旧 SOP vs 新 SOP）
[ ] benchmarks/score.py --bench B01 通过？
[ ] benchmarks/metrics.py ≥ 70%？
[ ] 硬规则改动 → 附 ≥1 个真实案例说明必要性？
```
这是唯一需要你亲自跑验证的。其他三类看文件就行。

### 5. Issue（不需要合代码）

**谁贡献：** 所有人，包括非技术用户

**提交什么：** GitHub Issue

**你审什么（1 分钟）：**
```
[ ] 描述清楚卡在哪一步了？
[ ] 附了 Phase清单截图或错误信息？
```
不用合——回复、讨论、确认是 bug 就开 PR 修。

---

## 合并优先级

```
knowledge_graph 条目  >  contrib 角色  >  benchmark 条目  >  SOP 改动

   最频繁，最轻量          中等             偶尔              慎重
   3分钟审一个            5分钟审一个      5分钟审一个        需要亲自跑
```

## 你的工作流

```
GitHub Notification → 看类型 → 对表审 → 合 / 标 ⚠️ / 要求补充

每天 10 分钟够了。大部分是 knowledge_graph 条目——跑完课题的人顺手交作业。
```
